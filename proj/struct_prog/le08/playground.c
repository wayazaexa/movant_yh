/*
    .
*/
#include <stdio.h>

int main ()
{
    printf("Oh hi Mark!\n\n");
    /* Just to try something for when Rickard said that you can declare an array w/o specifying size. As expected, he was wrong.
    int num[];
    int new;
    int i = 0;
    
    do
    {
        scanf("%d", new);
        num[i] = new;
        i++;
    } while (new != 0);
    */
    return 0;
}
